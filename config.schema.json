{
  "pluginAlias": "Homebridge Broadlink Window Covering",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "Homebridge Broadlink Window Covering Platform"
      },
      "logLevel": {
        "title": "Log Level",
        "type": "string",
        "default": "info",
        "enum": ["error", "warn", "info", "verbose", "debug", "silly"]
      }
    }
  }
}